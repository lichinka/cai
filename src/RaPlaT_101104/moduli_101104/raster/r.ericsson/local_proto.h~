#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <grass/gis.h>
#include <grass/display.h>
#include <grass/raster.h>
#include <grass/glocale.h>

/* main.c */
int do_profile(double, double, double, double, char *,  double, int, int,
	       char *, struct Cell_head win, double *,  double *);
   
/* read_rast.c */
int read_rast(double, double, double, int,  RASTER_MAP_TYPE, char *, struct Cell_head win);

/* max_prof_point.c*/
/* int max_prof_point()*/

/* input.c */
/*int input(char *, char *, char *, char *, char *);*/

/*#ifdef MAIN
int clr;
struct Colors colors;
#else
extern int clr;
extern struct Colors colors;
#endif*/
